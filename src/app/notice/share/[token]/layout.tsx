// Standalone layout for the public deep-link notice summary page.
// No sidebar, no auth header — just the bare page shell.
export default function NoticeShareLayout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
